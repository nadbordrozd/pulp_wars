export const RULESET7_PLAYTEST_UNIT_ART_IDS = Object.freeze({
  CAPTAIN: "unit-original-captain-v7r10",
} as const);

export const RULESET7_PLAYTEST_PORTRAIT_ART_IDS = Object.freeze({
  CAPTAIN: "portrait-original-captain-v7r10",
} as const);

/** Art-only registrations for the current mobility technologies. */
export const RULESET7_PLAYTEST_TECH_ART_IDS = Object.freeze({
  SCOUTING: "portrait-original-raider",
  RAIDING: "ui-action-pillage",
} as const);
